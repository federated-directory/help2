const o = ({
  workspace: i,
  document: e,
  path: r,
  method: n,
  getEntryByLocation: l
}) => {
  if (!i)
    return {
      title: "Untitled Tab"
    };
  if (e) {
    const t = l({
      document: e,
      path: r,
      method: n
    });
    return t ? t.type === "document" ? {
      title: t.title,
      icon: "document"
    } : {
      title: t.title,
      icon: "request"
    } : {
      title: "Untitled Tab"
    };
  }
  return {
    title: "Workspace"
  };
};
export {
  o as getTabDetails
};
