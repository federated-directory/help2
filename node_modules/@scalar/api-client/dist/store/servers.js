import { LS_KEYS as o } from "@scalar/helpers/object/local-storage";
import { serverSchema as u } from "@scalar/oas-utils/entities/spec";
import { mutationFactory as a } from "@scalar/object-utils/mutator-record";
import { reactive as m } from "vue";
function R(s) {
  const r = m({}), v = a(r, m({}), s && o.SERVER);
  return {
    servers: r,
    serverMutators: v
  };
}
function h({
  serverMutators: s,
  collections: r,
  collectionMutators: v,
  requests: f,
  requestMutators: d
}) {
  return {
    addServer: (S, e) => {
      const t = u.parse(S);
      return r[e] ? v.edit(e, "servers", [
        ...r[e].servers,
        t.uid
      ]) : f[e] && d.edit(e, "servers", [...f[e].servers, t.uid]), s.add(t), t;
    },
    deleteServer: (S, e) => {
      r[e] && (v.edit(
        e,
        "servers",
        r[e].servers.filter((t) => t !== S)
      ), s.delete(S));
    }
  };
}
export {
  R as createStoreServers,
  h as extendedServerDataFactory
};
