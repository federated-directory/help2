import type { ApiReferenceConfiguration } from '@scalar/types/api-reference';
import type { WorkspaceStore } from '@scalar/workspace-store/client';
import type { Ref } from 'vue';
import type { Api } from '../api.js';
import type { ApiMetadata } from '../entities/registry/document.js';
export declare function loadDocument({ namespace, slug, workspaceStore, registryDocuments, registryUrl, config, api, removable, }: {
    namespace: string;
    slug: string;
    workspaceStore: WorkspaceStore;
    registryDocuments: Ref<ApiMetadata[]>;
    registryUrl: string;
    config: Partial<ApiReferenceConfiguration>;
    api: Api;
    removable?: boolean;
}): Promise<{
    id: string;
    title: string;
    namespace: string;
    currentVersion: string;
    logoUrl: string | null;
    slug: string;
} | undefined>;
//# sourceMappingURL=add-documents-to-store.d.ts.map