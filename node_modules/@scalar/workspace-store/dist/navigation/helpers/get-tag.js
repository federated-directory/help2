const getTag = ({
  tagsMap,
  name,
  documentId,
  generateId
}) => {
  if (!tagsMap.get(name)) {
    tagsMap.set(name, {
      id: generateId({
        type: "tag",
        tag: { name },
        parentId: documentId
      }),
      parentId: documentId,
      entries: [],
      tag: { name }
    });
  }
  return tagsMap.get(name);
};
export {
  getTag
};
//# sourceMappingURL=get-tag.js.map
